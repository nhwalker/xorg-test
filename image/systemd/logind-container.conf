# Installed as /etc/systemd/logind.conf.d/10-container.conf inside the image.
# The desktop session service owns tty1; don't let the container's logind
# spawn gettys on VT activity or reserve a VT.
[Login]
NAutoVTs=0
ReserveVT=0
