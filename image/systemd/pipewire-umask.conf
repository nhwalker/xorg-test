# Installed as a drop-in for the pipewire and pipewire-pulse user services.
# The exported sockets in /run/desktop-audio must be connectable by other
# uids (host users, other containers); sockets inherit the daemon's umask.
[Service]
UMask=0000
