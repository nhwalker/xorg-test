# Allow the Xorg suid wrapper to run with root rights for any user.
# Inside the container there is no udev-based logind device handover across
# the container boundary to rely on, so Xorg opens /dev/dri, /dev/input and
# the VT directly with root privileges.
allowed_users = anybody
needs_root_rights = yes
