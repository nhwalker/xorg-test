# Part of the desktop-container deploy tree (deploy/README.md).
# Route PulseAudio clients on the host to the containerized desktop's
# pipewire-pulse socket, and never autospawn a competing daemon.
default-server = unix:/run/desktop-audio/pulse
autospawn = no
